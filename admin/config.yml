# ============================================================
#  AL SYED FRAGRANCE — Decap CMS Configuration
#  Accessible at: /admin
# ============================================================

backend:
  name: git-gateway
  branch: main

media_folder: "public/uploads"
public_folder: "/uploads"

collections:

  - name: "settings"
    label: "⚙️ Site Settings"
    files:
      - name: "general"
        label: "General Settings"
        file: "_data/settings.json"
        fields:
          - label: "Brand Name"
            name: "brand_name"
            widget: "string"
            default: "AL SYED FRAGRANCE"
            hint: "Displayed in the header and footer."

          - label: "Tagline / Slogan"
            name: "tagline"
            widget: "string"
            default: "Our goal is to make premium and well-loved fragrances affordable."

          - label: "Logo"
            name: "logo"
            widget: "image"
            hint: "Upload your logo. Recommended: PNG with transparent background, max 500px wide."
            required: false

          - label: "WhatsApp Order Number"
            name: "whatsapp"
            widget: "string"
            default: "923285760236"
            hint: "Country code + number, no spaces or dashes. E.g. 923285760236"

          - label: "Instagram Handle"
            name: "instagram"
            widget: "string"
            default: "al_syed_fragrance"
            hint: "Without the @ symbol."

          - label: "Facebook Page"
            name: "facebook"
            widget: "string"
            default: "Alsyedfragrance"

          - label: "Contact Email"
            name: "email"
            widget: "string"
            default: "alsyedfragrance85@gmail.com"

  - name: "hero"
    label: "🖼️ Hero / Cover Section"
    files:
      - name: "hero_main"
        label: "Hero Section"
        file: "_data/hero.json"
        fields:
          - label: "Hero Background Image"
            name: "image"
            widget: "image"
            hint: "Upload a wide landscape image (recommended 1920×1080px or larger)."
            required: false

          - label: "Main Headline"
            name: "headline"
            widget: "string"
            default: "AL SYED"
            hint: "Large text displayed in the hero area."

          - label: "Sub-Headline"
            name: "subheadline"
            widget: "string"
            default: "Fragrance"

          - label: "Sub-Text (small)"
            name: "subtext"
            widget: "string"
            default: "Luxury Scents · Timeless Identity · Affordable Elegance"

          - label: "Call-to-Action Button Text"
            name: "cta_text"
            widget: "string"
            default: "Explore Collection"

  - name: "products"
    label: "🧴 Products"
    label_singular: "Product"
    folder: "_data/products"
    create: true
    format: "json"
    extension: "json"
    slug: "{{slug}}"
    identifier_field: "name"
    summary: "{{name}} — {{category}} — Rs {{price}}"
    sortable_fields: ["name", "category", "price"]
    fields:
      - label: "Product Name"
        name: "name"
        widget: "string"
        hint: "e.g. Oud Royale"

      - label: "Category"
        name: "category"
        widget: "select"
        options:
          - { label: "Men", value: "men" }
          - { label: "Women", value: "women" }
          - { label: "New Arrival", value: "new-arrival" }
          - { label: "Bundle Offer", value: "bundle" }
          - { label: "Best Seller", value: "best-seller" }
        default: "new-arrival"

      - label: "Product Image"
        name: "image"
        widget: "image"
        hint: "Upload a product photo. Square or portrait (3:4) ratio looks best."
        required: false

      - label: "Price (30ml) — Rs"
        name: "price"
        widget: "number"
        value_type: "int"
        min: 1
        hint: "Price in Pakistani Rupees for the 30ml size."

      - label: "Price (50ml) — Rs"
        name: "price_50"
        widget: "number"
        value_type: "int"
        min: 1
        required: false
        hint: "Leave blank for bundle products or if 50ml is not available."

      - label: "Badge / Label"
        name: "badge"
        widget: "string"
        required: false
        hint: "Optional short badge shown on card, e.g. 'New', 'Popular', '#1 Seller'."

      - label: "Product Description"
        name: "description"
        widget: "text"
        hint: "2–3 sentences describing the scent, notes, and occasion."

      - label: "Published"
        name: "published"
        widget: "boolean"
        default: true
        hint: "Uncheck to hide this product without deleting it."
